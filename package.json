{
  "name": "prolocalbuilder",
  "version": "0.1.0",
  "private": true,
  "description": "ProLocalBuilder monorepo",
  "scripts": {
    "dev:web": "pnpm --filter @prolocalbuilder/web dev",
    "build:web": "pnpm --filter @prolocalbuilder/web build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@11.1.1"
}
